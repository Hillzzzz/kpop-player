generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("postgresql://postgres:nVpUZhviySfOtTPVnQaarHTyqQyrbpAk@trolley.proxy.rlwy.net:23845/railway")
}

model Song {
  id        Int      @id @default(autoincrement())
  title     String
  artist    String
  album     String?
  year      Int?
  coverUrl  String?
  audioUrl  String
  createdAt DateTime @default(now())
}

